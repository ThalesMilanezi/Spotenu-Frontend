  const initialState = {
    Allgenders: []
  }

  const gender = (state = initialState, action) => {
    switch(action.type) {
      case "":
        return{

        }
        default:
      return state
    }
  }

  export default gender